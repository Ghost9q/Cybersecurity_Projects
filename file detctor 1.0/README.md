File Detector:
This is a simple file detector made with python, to detect the real file type of a file,
regardless of it's file extension. It first reads the header of a file to find it's magic
number and compares it to a database of known file signatures or magic nubmbers. 
